// Solução para o problema 23.
#include <stdio.h>

void main(void)
{
    char a[51], b[51], c[51];
    scanf("%s", a);
    scanf("%s", b);
    scanf("%s", c);

    puts(c);
    puts(b);
    puts(a);
}
