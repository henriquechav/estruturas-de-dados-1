// Solução para o problema 22.
#include <stdio.h>

void main(void)
{
    char a[51], b[51], c[51];
    scanf("%s", a);
    scanf("%s", b);
    scanf("%s", c);

    puts(a);
    puts(b);
    puts(c);
}
